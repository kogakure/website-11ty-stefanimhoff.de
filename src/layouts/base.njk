<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{{ title }}</title>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preload" href="{{ '/assets/fonts/secuela-regular.woff2' | url }}" as="font" type="font/woff2" crossorigin />
    <link rel="preload" href="{{ '/assets/fonts/secuela-italic.woff2' | url }}" as="font" type="font/woff2" crossorigin />
    <link rel="stylesheet" href="{{ '/assets/styles/main.css' | url }}" />
  </head>
  <body>
    {% include "navigation.njk" %}
    <button onClick={window.__toggleTheme()}>Toggle Theme</button>
    <main>
      {{ content | safe }}
    </main>
    {% include "scripts.njk" %}
  </body>
</html>
